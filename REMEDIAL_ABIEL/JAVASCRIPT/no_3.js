/** @format */

function konversi(nilai, rate) {
  var kenaikan = nilai * rate;
  var hasil = console.log(`konversi mata uang saat ini dengan rate ${rate} adalah ${kenaikan}`);
  return hasil;
}

konversi(1000, 2.2);
